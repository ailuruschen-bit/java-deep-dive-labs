# Environment inspection

This lab records which Java runtime and compiler the current shell resolves before any project structure or build tool is introduced.

Run:

```bash
./inspect-environment.sh
```

The script deliberately does not install or select a JDK. It only observes the current shell environment.
