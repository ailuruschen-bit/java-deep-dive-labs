# 文章目录

这里保存文章的可编辑正文。`outline.md` 负责维护整体结构，`chapters/` 中的文件是各章正文，`assets/` 用于图片和图表。

正文文件与实验目录使用相同的两位数编号，例如：

```text
article/chapters/02-memory-model.md
chapters/02-memory-model/
```

这样引用代码、复现实验和审阅章节时都能快速定位。发布到 Zenn 前，可根据最终形态将各章合并为单篇文章，或映射为 Zenn Book 的多个章节。
